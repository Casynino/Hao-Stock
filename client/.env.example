# Base URL of the Hao Stock API. In dev, Vite proxies /api to the backend, so
# the default below works without changes. In production set this to your
# deployed API origin, e.g. https://api.haostock.co.tz/api
VITE_API_URL=/api
